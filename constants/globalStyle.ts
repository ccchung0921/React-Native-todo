import { StyleSheet } from "react-native"

// for reuse style
export const styles = StyleSheet.create({
  sectionTitle: {
    fontSize: 16,
    margin: 10,
  },
  sectionScrollView: {
    flex: 1,
    marginHorizontal: 5,
  },
})
